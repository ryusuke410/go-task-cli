version: '3'

tasks:
  default:
    desc: task list
    cmds:
      - ./task-bin/task --list
    silent: true

includes:
  cspell:
    dir: ./cspell
    taskfile: ./cspell/Taskfile.yml
  cli:
    dir: ./packages/go-task-cli
    taskfile: ./packages/go-task-cli/Taskfile.yml
